<Window x:Class="AppconLogin.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppconLogin"
        mc:Ignorable="d"
        Title="Introduzca su Usuario y Contraseña" 
        Height="150" Width="320">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Usuario" Margin="5" Grid.Column="0" Grid.Row="0"/>
        <Label Content="Contraseña" Margin="5" Grid.Column="0" Grid.Row="1"/>
        <TextBox Name="user" Margin="5" Grid.Column="1" Grid.Row="0"/>
        <PasswordBox Name="password" Margin="5" Grid.Column="1" Grid.Row="1" />
        <Button Name="check" Content="OK" Width="100" Margin="5" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" Click="check_Click" />
    </Grid>
</Window>
